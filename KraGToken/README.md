# Modification of the ERC20 Token standard

Requirements:
    
    truffle
    ganache-cli


To run this version of the software:
    Run a local blockchain such as Test RPC, Ganache, `ganache-cli` etc.
    
    truffle compile
    truffe migrate
